node_modules/
dist/
ios/

# Android: track the native project, but never commit build outputs or signing material.
# (android/.gitignore handles build/, local.properties, generated capacitor config, etc.)
android/app/release.keystore
android/**/*.keystore
android/**/*.jks
.DS_Store
*.local
.env
# Local Worker secrets for `wrangler dev` (SESSION_SECRET, ADMIN_SECRET…).
# Real secrets — never commit. Deployed envs use `wrangler secret put` instead.
.dev.vars
server/.dev.vars
server/.wrangler
# Vite dep-optimizer cache for `npm run admin` (kept out of the game's own cache)
admin/.vite/
todo/
.superpowers/
.worktrees/
.claude/env.md
.worktrees/
.temp/
screenshots/
.playwright-mcp/
StorelistingScreenshots/
# DPR gate screenshot artifact (regenerated each run)
dpr-gate.png

# Load-test fixtures — contains generated player secrets, regenerate with `npm run loadtest:seed`
loadtest/fixtures.json

# Load-test run summaries — regenerated on every run
loadtest/results/

# scratch output from `render-icon.mjs --variants`
.icon-variants/
.wrangler/state/