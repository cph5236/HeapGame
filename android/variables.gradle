ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
    // Overrides @capacitor-community/admob's floating '24.9.+'. Pinned so CI
    // builds are reproducible, and on 25.x because 24.9 applied the
    // deprecated LAYOUT_IN_DISPLAY_CUTOUT_MODE_SHORT_EDGES unconditionally on
    // API 28+; 25.x gates it to API <= 34. Play Console flagged this.
    playServicesAdsVersion = '25.4.0'
}